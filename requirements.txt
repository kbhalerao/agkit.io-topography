boto3>=1.34
botocore>=1.34
requests>=2.31
# Pinned <2: the base image's GDAL `gdal_array` extension is compiled
# against NumPy 1.x and segfaults on import under NumPy 2.x — which breaks
# every ReadAsArray()/WriteArray() call (reducers, geoworker, geocolorize).
numpy>=1.26,<2
